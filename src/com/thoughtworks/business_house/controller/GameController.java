package com.thoughtworks.business_house.controller;

import java.util.Scanner;

import com.thoughtworks.business_house.player_entities.Player;

public class GameController {

	private final int maxChances = 10;
	private final int initialWalletAmount = 1000;
	public static void main(String[] args) {
		Scanner sc  = new Scanner(System.in);
		System.out.println("Enter number of players: ");
		int numOfPlayers = sc.nextInt();
		Player [] players = new Player[numOfPlayers];
		for(int i =0; i < players.length; i++) {
			Player player = new Player();
			player.setWallet(wallet);
			players
		}
		System.out.println("Enter cells: ");
		String cells = sc.next();
		new Boards().createB
		for( i : turn) {
			for(Player: players) {
				player.r
			}
		}
		
	}

}
